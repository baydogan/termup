.idea/
.DS_Store
CLAUDE.md

# compiled binaries (root-level only; cmd/termup* dirs are anchored out)
/termup
/termupd
/bin/

# the live target list is per-installation; the tracked template is
# config.example.yaml (see also testdata/manual-cases.yaml)
/config.yaml

# local dev database (sqlite writes sidecar files next to it)
/termup.db
/termup.db-*

# coverage profile (CI writes it; see .github/workflows/ci.yml)
/coverage.out
